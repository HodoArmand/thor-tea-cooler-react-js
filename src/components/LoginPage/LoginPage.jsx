import useDarkMode from '../../common/useDarkMode'

function LoginPage() {

    useDarkMode("login-body");

    return (
        <div>
            Login
        </div>
    )
}

export default LoginPage
